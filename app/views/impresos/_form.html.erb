<%= form_with(model: impreso, local: true) do |form| %>
  <% if impreso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(impreso.errors.count, "error") %> prohibited this impreso from being saved:</h2>

      <ul>
      <% impreso.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :personas_id %>
    <%= form.number_field :personas_id, id: :impreso_personas_id %>
  </div>

  <div class="field">
    <%= form.label :denuncium_id %>
    <%= form.text_field :denuncium_id, id: :impreso_denuncium_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :impreso_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
